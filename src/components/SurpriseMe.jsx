import { useState } from 'react'; import { useNavigate } from 'react-router-dom'; import { Dices } from 'lucide-react'; import { useMovieShelf } from '../context/MovieShelfContext';
export default function SurpriseMe(){const [picking,setPicking]=useState(false);const navigate=useNavigate(),{movies}=useMovieShelf();const choose=()=>{if(picking||!movies.length)return;setPicking(true);setTimeout(()=>navigate(`/movies/${movies[Math.floor(Math.random()*movies.length)].id}`),650)};return <section className="surprise"><p className="eyebrow">DON'T KNOW WHAT TO WATCH?</p><h2>Let me choose<br/>for you.</h2><button disabled={!movies.length} className={picking?'picking':''} onClick={choose}><Dices size={20}/> {picking?'Shuffling...':'Surprise Me'}</button></section>}
