import { Link } from 'react-router-dom'; import { ArrowUpRight, Star } from 'lucide-react';
export default function MovieCard({movie}){return <article className="movie-card"><Link to={`/movies/${movie.id}`} className="poster"><img src={movie.poster} alt={`${movie.title} artwork`}/><div className="card-overlay"><span>View movie <ArrowUpRight size={16}/></span></div></Link><div className="card-info"><div><h3>{movie.title}</h3><p>{movie.year} · {movie.genres?.[0]||'Film'}</p></div><span className="rating"><Star size={13} fill="currentColor"/> {movie.rating}</span></div></article>}
