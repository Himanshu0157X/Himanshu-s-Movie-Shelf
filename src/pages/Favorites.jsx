import { Heart } from 'lucide-react'; import MovieGrid from '../components/MovieGrid'; import { useMovieShelf } from '../context/MovieShelfContext';
export default function Favorites(){const {movies}=useMovieShelf(),favorites=movies.filter(m=>m.favorite);return <div className="page"><p className="eyebrow">THE ONES I LOVE MOST</p><h1>Personal <em>favourites.</em></h1><p className="intro">The movies that linger long after the credits. No algorithm, just feeling.</p>{favorites.length?<MovieGrid movies={favorites}/>:<div className="empty"><Heart/><h2>The shelf is waiting</h2><p>No favourites have been marked yet.</p></div>}</div>}
